;字符串中的所有大写字母改为小写字母
;0为截止符

DSEG SEGMENT
	STRING	DB	'HOW are yoU !', 0
DSEG ENDS

SSEG SEGMENT STACK

SSEG ENDS

CSEG SEGMENT
	ASSUME	CS:CSEG, DS:DSEG, SS:SSEG
START:
	MOV AX, DSEG
	MOV DS, AX
	MOV	AX, SSEG
	MOV SS, AX 
	
	MOV	SI, OFFSET STRING
S:	
	MOV AL, [SI]
	CMP AL, 0
	JZ 	EXIT
	OR AL, 20H
	MOV [SI], AL 
	INC SI
	JMP S
	
EXIT:
	MOV AX, 4C00H
	INT 21H

CSEG ENDS
END START