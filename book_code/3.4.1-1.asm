;16x+y

DSEG SEGMENT
	XXX	DW	1234H
	YYY	DW	5678H
	ZZZ	DD	0  ;保存结果
DSEG ENDS


SSEG SEGMENT STACK

SSEG ENDS

CSEG SEGMENT
	ASSUME CS:CSEG, DS:DSEG, SS:SSEG
START:
	MOV AX, DSEG
	MOV DS, AX 
	
	MOV AX, SSEG
	MOV SS, AX 
	
	MOV CX, 16
	MOV AX, XXX
	MUL	CX   ;DX:AX存放结果
	
	MOV AX, YYY
	ADC DX, 0
	
	MOV WORD PTR ZZZ, AX
	MOV WORD PTR ZZZ+2, DX 
	
	
	MOV AX, 4C00H
	INT 21H

CSEG ENDS
END START